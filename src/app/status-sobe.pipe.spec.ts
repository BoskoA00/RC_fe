import { StatusSobePipe } from './status-sobe.pipe';

describe('StatusSobePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusSobePipe();
    expect(pipe).toBeTruthy();
  });
});
